# Protocol Fixtures

Fixtures in this directory should come from the upstream app-server README,
generated JSON Schema samples, or intentionally reduced examples that exercise
CoKit protocol parsing.

Do not store secrets, access tokens, private paths, account identifiers, or
auth URLs in fixtures.
